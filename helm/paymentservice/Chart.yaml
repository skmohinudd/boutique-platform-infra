# BOUTIQUE-DOCS-BEGIN
# Helm Chart metadata.
# - apiVersion: Helm chart specification version.
# - name: logical chart/application name.
# - version: version of this Helm chart itself.
# - appVersion: informational application version; CI/CD image.tag is authoritative.
# BOUTIQUE-DOCS-END

apiVersion: v2
name: paymentservice
description: Boutique DEV Helm chart for paymentservice
type: application
version: 0.1.0
appVersion: "1.0.2"
