apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMapName }}
data:
  SERVER_PORT: "{{ .Values.config.serverPort }}"
  CORS_ALLOWED_ORIGINS: "{{ .Values.config.corsAllowedOrigins }}"
  KAFKA_BOOTSTRAP_SERVERS: "{{ .Values.config.kafkaBootstrapServers }}"
  KAFKA_ORDER_EVENTS_TOPIC: "{{ .Values.config.kafkaOrderEventsTopic }}"
  SPRING_RABBITMQ_HOST: "{{ .Values.config.rabbitmqHost }}"
  SPRING_RABBITMQ_PORT: "{{ .Values.config.rabbitmqPort }}"
  OUTBOX_PUBLISH_DELAY_MS: "{{ .Values.config.outboxPublishDelayMs }}"
  OPENAPI_ENABLED: "{{ .Values.config.openapiEnabled }}"
  SWAGGER_ENABLED: "{{ .Values.config.swaggerEnabled }}"
